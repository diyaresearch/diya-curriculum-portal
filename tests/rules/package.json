{
  "name": "firestore-rules-tests",
  "private": true,
  "description": "Security rules tests for portal-app/firestore.rules (#419, #420, #382)",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --config ../../firebase.json --project rules-test-curriculum-portal \"jest --runInBand\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.0.0",
    "firebase-tools": "^14.0.0",
    "jest": "^29.7.0"
  }
}