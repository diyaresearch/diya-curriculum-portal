# Testing dependencies for DIYA Curriculum Portal API tests
pytest>=7.0.0
requests>=2.25.0
pytest-asyncio>=0.21.0
pytest-cov>=4.0.0
pytest-html>=3.0.0
# Backs pytest.ini's `timeout = 300` - without this installed, that setting
# is silently ignored rather than bounding a hung request (#436).
pytest-timeout>=2.1.0
